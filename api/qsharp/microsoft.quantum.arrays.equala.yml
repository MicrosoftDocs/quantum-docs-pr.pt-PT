### YamlMime:QSharpType
uid: microsoft.quantum.arrays.equala
name: EqualA
type: function
namespace: Microsoft.Quantum.Arrays
summary: Dadas duas matrizes do mesmo tipo e um predicado que é definido para pares de elementos das matrizes, verifica se as matrizes são iguais.
remarks: >-
  Esta função é definida para tipos genéricos; isto é, sempre que temos `'T[]` duas matrizes e uma função, `equal: ('T, 'T) -> Bool`esta função devolve um `Bool` valor que indica se as matrizes são iguais.

  Para que duas matrizes sejam consideradas iguais, têm de ter o mesmo comprimento e os elementos nas mesmas posições nas matrizes têm de ser iguais.
examples: >-
  O código seguinte verifica se diferentes pares de matrizes são iguais:


  ```qsharp

  open Microsoft.Quantum.Arrays;

  open Microsoft.Quantum.Logical;


  function EqualADemo() : Unit {
      let equalArrays = EqualA(EqualI, [2, 3, 4], [2, 3, 4]);
      let differentLength = EqualA(EqualD, [2.0, 3.0, 4.0], [2.0, 3.0]);
      let differentElements = EqualA(EqualR, [One, Zero], [One, One]);
      Message($"Equal arrays are {equalArrays ? "equal" | "not equal"}");
      Message($"Arrays of different length are {differentLength ? "equal" | "not equal"}");
      Message($"Arrays of the same length with different elements are {differentElements ? "equal" | "not equal"}");
  }

  ```
syntax: "function EqualA (equal : (('T, 'T) -> Bool), array1 : 'T[], array2 : 'T[]) : Bool"
input:
  content: "(equal : (('T, 'T) -> Bool), array1 : 'T[], array2 : 'T[])"
  types:
  - name: equal
    isFunction: true
    input:
      types:
      - uid: "'T"
        isLocal: true
      - uid: "'T"
        isLocal: true
    output:
      types:
      - isPrimitive: true
        uid: Bool
  - name: array1
    summary: A primeira matriz a ser comparada.
    isArray: true
    uid: "'T"
    isLocal: true
  - name: array2
    summary: A segunda matriz a ser comparada.
    isArray: true
    uid: "'T"
    isLocal: true
output:
  content: Bool
  types:
  - summary: >-
      O `true` valor se `array1` e `array2` apenas se e forem iguais.

      Isto é, se ambas as matrizes tiverem o mesmo `equal`comprimento, e todos os elementos forem iguais aos definidos por .
    isPrimitive: true
    uid: Bool
typeParameters:
- uid: "'T"
  summary: O tipo de elementos de cada matriz.
metadata:
  ms.openlocfilehash: d2af5ff8e2d66e6162c73c3854670451a00dcf50
  ms.sourcegitcommit: db23885adb7ff76cbf8bd1160d401a4f0471e549
  ms.translationtype: MT
  ms.contentlocale: pt-PT
  ms.lasthandoff: 05/01/2020
  ms.locfileid: "82676489"
