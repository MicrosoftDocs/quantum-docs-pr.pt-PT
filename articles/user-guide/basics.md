---
title: ':::no-loc(Q#)::: Básico'
description: 'Conceitos básicos de :::no-loc(Q#):::'
author: gillenhaalb
ms.author: a-gibec
ms.date: 02/28/2020
ms.topic: article
uid: microsoft.quantum.guide.basics
no-loc:
- ':::no-loc(Q#):::'
- ':::no-loc($$v):::'
ms.openlocfilehash: b3bc0841eabeac5d3968776f9dab3a02b1a1eef9
ms.sourcegitcommit: 29e0d88a30e4166fa580132124b0eb57e1f0e986
ms.translationtype: MT
ms.contentlocale: pt-PT
ms.lasthandoff: 10/27/2020
ms.locfileid: "92691626"
---
# <a name="no-locq-basics"></a><span data-ttu-id="e6601-103">:::no-loc(Q#)::: Básico</span><span class="sxs-lookup"><span data-stu-id="e6601-103">:::no-loc(Q#)::: Basics</span></span>

<span data-ttu-id="e6601-104">Este artigo apresenta uma breve introdução aos blocos básicos de construção de :::no-loc(Q#)::: .</span><span class="sxs-lookup"><span data-stu-id="e6601-104">This article presents a brief introduction to the basic building blocks of :::no-loc(Q#):::.</span></span>

<span data-ttu-id="e6601-105">Para uma visão geral do que :::no-loc(Q#)::: é e onde se enquadra como um componente fundamental do Kit de Desenvolvimento Quântico, veja [o que :::no-loc(Q#)::: é?](xref:microsoft.quantum.overview.q-sharp)</span><span class="sxs-lookup"><span data-stu-id="e6601-105">For an overview of what :::no-loc(Q#)::: is and where it fits in as a fundamental component of the Quantum Development Kit, see [What is :::no-loc(Q#):::?](xref:microsoft.quantum.overview.q-sharp).</span></span> 

## <a name="what-is-a-quantum-program"></a><span data-ttu-id="e6601-106">O que é um programa quântico?</span><span class="sxs-lookup"><span data-stu-id="e6601-106">What is a quantum program?</span></span>

<span data-ttu-id="e6601-107">Do ponto de vista técnico, um programa quântico é um conjunto particular de sub-rotinas clássicas que, quando chamados, realizam certas operações num sistema quântico.</span><span class="sxs-lookup"><span data-stu-id="e6601-107">From a technical perspective, a quantum program is a particular set of classical subroutines which, when called, perform certain operations on a quantum system.</span></span>
<span data-ttu-id="e6601-108">Uma consequência importante dessa visão é que um :::no-loc(Q#)::: programa não modela diretamente os qubits, mas descreve como um computador controlado clássicamente interage com esses qubits.</span><span class="sxs-lookup"><span data-stu-id="e6601-108">An important consequence of that view is that a :::no-loc(Q#)::: program does not directly model qubits themselves, but rather describes how a classically controlled computer interacts with those qubits.</span></span>
<span data-ttu-id="e6601-109">Por design, :::no-loc(Q#)::: não define estados quânticos ou outras propriedades da mecânica quântica diretamente.</span><span class="sxs-lookup"><span data-stu-id="e6601-109">By design, :::no-loc(Q#)::: does not define quantum states or other properties of quantum mechanics directly.</span></span>
<span data-ttu-id="e6601-110">Por exemplo, considere o estado $\ket{+} = \left {0} (\ket + \ket {1} \right) / \sqrt {2} $ discutido no guia quantum [Computing Concepts.](xref:microsoft.quantum.concepts.intro)</span><span class="sxs-lookup"><span data-stu-id="e6601-110">For instance, consider the state $\ket{+} = \left(\ket{0} + \ket{1}\right) / \sqrt{2}$ discussed in the [Quantum Computing Concepts](xref:microsoft.quantum.concepts.intro) guide.</span></span>
<span data-ttu-id="e6601-111">Para preparar este estado em :::no-loc(Q#)::: , comece com os fatos de que os qubits são inicializados no estado $\ket$ e que {0} $\ket{+} = H\ket {0} $, onde $H$ é a [transformação Hadamard,](xref:microsoft.quantum.glossary#hadamard)implementada pela [ `H` operação](xref:Microsoft.Quantum.Intrinsic.H).</span><span class="sxs-lookup"><span data-stu-id="e6601-111">To prepare this state in :::no-loc(Q#):::, start with the facts that the qubits are initialized in the $\ket{0}$ state, and that $\ket{+} = H\ket{0}$, where $H$ is the [Hadamard transform](xref:microsoft.quantum.glossary#hadamard), implemented by the [`H` operation](xref:Microsoft.Quantum.Intrinsic.H).</span></span> <span data-ttu-id="e6601-112">O código básico :::no-loc(Q#)::: para inicializar e transformar um qubit, então, é assim:</span><span class="sxs-lookup"><span data-stu-id="e6601-112">The basic :::no-loc(Q#)::: code to initialize and transform a qubit, then, looks like this:</span></span>

```qsharp
using (qubit = Qubit()) {
    // At this point, the qubit is in the state |0⟩.
    H(qubit);
    // H is now applied, such that the qubit is in H|0⟩ = |+⟩, as desired.
}
```
<span data-ttu-id="e6601-113">Para obter mais informações sobre a inicialização, ou *a atribuição,* qubits, consulte [Trabalhar com qubits.](xref:microsoft.quantum.guide.qubits)</span><span class="sxs-lookup"><span data-stu-id="e6601-113">For more information on initializing, or *allocating* , qubits, see [Working with qubits](xref:microsoft.quantum.guide.qubits).</span></span>

## <a name="quantum-states-in-no-locq"></a><span data-ttu-id="e6601-114">Estados quânticos em :::no-loc(Q#):::</span><span class="sxs-lookup"><span data-stu-id="e6601-114">Quantum states in :::no-loc(Q#):::</span></span>

<span data-ttu-id="e6601-115">Importante, o programa anterior não se refere explicitamente ao estado interior, mas descreveu como o :::no-loc(Q#)::: nosso programa *transformou* o Estado.</span><span class="sxs-lookup"><span data-stu-id="e6601-115">Importantly, the previous program does not explicitly refer to the state within :::no-loc(Q#)::: but described how our program *transformed* the state.</span></span>
<span data-ttu-id="e6601-116">Com esta abordagem, você pode ser inteiramente agnóstico sobre o que *é* um estado quântico mesmo em cada máquina alvo, que pode ter diferentes interpretações dependendo da máquina.</span><span class="sxs-lookup"><span data-stu-id="e6601-116">With this approach, you can be entirely agnostic about what a quantum state even *is* on each target machine, which might have different interpretations depending on the machine.</span></span> 

<span data-ttu-id="e6601-117">Um :::no-loc(Q#)::: programa não pode introspeção no estado de um qubit.</span><span class="sxs-lookup"><span data-stu-id="e6601-117">A :::no-loc(Q#)::: program cannot introspect into the state of a qubit.</span></span>
<span data-ttu-id="e6601-118">Em vez disso, um programa pode chamar operações como [`Measure`](xref:Microsoft.Quantum.Intrinsic.Measure) aprender informações de um qubit, e ligar operações como [`X`](xref:Microsoft.Quantum.Intrinsic.X) e agir sobre o estado de um [`H`](xref:Microsoft.Quantum.Intrinsic.H) qubit.</span><span class="sxs-lookup"><span data-stu-id="e6601-118">Instead, a program can call operations such as [`Measure`](xref:Microsoft.Quantum.Intrinsic.Measure) to learn information from a qubit, and call operations such as [`X`](xref:Microsoft.Quantum.Intrinsic.X) and [`H`](xref:Microsoft.Quantum.Intrinsic.H) to act on the state of a qubit.</span></span>
<span data-ttu-id="e6601-119">O que estas operações realmente *fazem* é apenas feito concreto pela máquina-alvo usada para executar o :::no-loc(Q#)::: programa particular.</span><span class="sxs-lookup"><span data-stu-id="e6601-119">What these operations actually *do* is only made concrete by the target machine used to run the particular :::no-loc(Q#)::: program.</span></span>
<span data-ttu-id="e6601-120">Por exemplo, se executar o programa no nosso [simulador de estado completo,](xref:microsoft.quantum.machines.full-state-simulator)o simulador executa as operações matemáticas correspondentes ao sistema quântico simulado.</span><span class="sxs-lookup"><span data-stu-id="e6601-120">For example, if running the program on our [full-state simulator](xref:microsoft.quantum.machines.full-state-simulator), the simulator performs the corresponding mathematical operations to the simulated quantum system.</span></span>
<span data-ttu-id="e6601-121">Mas olhando para o futuro, quando a máquina-alvo é um verdadeiro computador quântico, chamando tais operações em :::no-loc(Q#)::: direcione o computador quântico para realizar as operações *reais* correspondentes no sistema quântico *real,* por exemplo, impulsos laser precisamente cronometrado).</span><span class="sxs-lookup"><span data-stu-id="e6601-121">But looking toward the future, when the target machine is a real quantum computer, calling such operations in :::no-loc(Q#)::: directs the quantum computer to perform the corresponding *real* operations on the *real* quantum system, for example, precisely timed laser pulses).</span></span>

<span data-ttu-id="e6601-122">Um :::no-loc(Q#)::: programa recombina estas operações tal como definido por uma máquina-alvo para criar novas operações de alto nível para expressar a computação quântica.</span><span class="sxs-lookup"><span data-stu-id="e6601-122">A :::no-loc(Q#)::: program recombines these operations as defined by a target machine to create new, higher-level operations to express quantum computation.</span></span>
<span data-ttu-id="e6601-123">Desta forma, :::no-loc(Q#)::: torna-se fácil expressar a lógica subjacente aos algoritmos quânticos e híbridos quânticos-clássicos, ao mesmo tempo que é geral no que diz respeito à estrutura de uma máquina-alvo ou simulador.</span><span class="sxs-lookup"><span data-stu-id="e6601-123">In this way, :::no-loc(Q#)::: makes it easy to express the logic underlying quantum and hybrid quantum–classical algorithms, while also being general with respect to the structure of a target machine or simulator.</span></span>

## <a name="no-locq-operations-and-functions"></a><span data-ttu-id="e6601-124">:::no-loc(Q#)::: operações e funções</span><span class="sxs-lookup"><span data-stu-id="e6601-124">:::no-loc(Q#)::: operations and functions</span></span>

<span data-ttu-id="e6601-125">Concretamente, um :::no-loc(Q#)::: programa compreende *operações,* *funções* e quaisquer tipos definidos pelo utilizador.</span><span class="sxs-lookup"><span data-stu-id="e6601-125">Concretely, a :::no-loc(Q#)::: program comprises *operations* , *functions* , and any user-defined types.</span></span> 

<span data-ttu-id="e6601-126">As operações são usadas para descrever as transformações dos sistemas quânticos e são o bloco de construção mais fundamental dos :::no-loc(Q#)::: programas.</span><span class="sxs-lookup"><span data-stu-id="e6601-126">Operations are used to describe the transformations of quantum systems and are the most fundamental building block of :::no-loc(Q#)::: programs.</span></span> <span data-ttu-id="e6601-127">Cada operação definida :::no-loc(Q#)::: pode então chamar qualquer número de outras operações.</span><span class="sxs-lookup"><span data-stu-id="e6601-127">Each operation defined in :::no-loc(Q#)::: may then call any number of other operations.</span></span>

<span data-ttu-id="e6601-128">Ao contrário das operações, as funções são usadas para descrever comportamentos clássicos puramente *deterministas* e não têm quaisquer efeitos além de calcular valores clássicos.</span><span class="sxs-lookup"><span data-stu-id="e6601-128">In contrast to operations, functions are used to describe purely *deterministic* classical behavior and do not have any effects besides computing classical values.</span></span> <span data-ttu-id="e6601-129">Por exemplo, suponha que queira medir os qubits no final de um programa e adicione os resultados da medição a uma matriz.</span><span class="sxs-lookup"><span data-stu-id="e6601-129">For example, suppose you want to measure the qubits at the end of a program and add the measurement results to an array.</span></span>
<span data-ttu-id="e6601-130">Neste caso, `Measure` é uma *operação* que instrui a máquina-alvo a efetuar uma medição nos qubits (reais ou simulados).</span><span class="sxs-lookup"><span data-stu-id="e6601-130">In this case, `Measure` is an *operation* that instructs the target machine to perform a measurement on the (real or simulated) qubits.</span></span> <span data-ttu-id="e6601-131">Ao mesmo tempo, as *funções* lidam com o processo clássico de adicionar os resultados devolvidos a uma matriz.</span><span class="sxs-lookup"><span data-stu-id="e6601-131">At the same time, *functions* handle the classical process of adding the returned results to an array.</span></span>

<span data-ttu-id="e6601-132">Juntos, as operações e funções são conhecidas como *callables* .</span><span class="sxs-lookup"><span data-stu-id="e6601-132">Together, operations and functions are known as *callables* .</span></span> <span data-ttu-id="e6601-133">A sua estrutura e comportamento subjacentes são introduzidos e detalhados em [Operações e :::no-loc(Q#)::: Funções em ](xref:microsoft.quantum.guide.operationsfunctions).</span><span class="sxs-lookup"><span data-stu-id="e6601-133">Their underlying structure and behavior are introduced and detailed in [Operations and Functions in :::no-loc(Q#):::](xref:microsoft.quantum.guide.operationsfunctions).</span></span>


## <a name="no-locq-syntax-overview"></a><span data-ttu-id="e6601-134">:::no-loc(Q#)::: visão geral da sintaxe</span><span class="sxs-lookup"><span data-stu-id="e6601-134">:::no-loc(Q#)::: syntax overview</span></span>

<span data-ttu-id="e6601-135">A sintaxe de uma linguagem descreve as diferentes combinações de símbolos que formam um programa sintáticamente correto.</span><span class="sxs-lookup"><span data-stu-id="e6601-135">The syntax of a language describes the different combinations of symbols that form a syntactically correct program.</span></span>
<span data-ttu-id="e6601-136">Em :::no-loc(Q#)::: , elementos de sintaxe são classificados em três grupos diferentes: tipos, expressões e declarações.</span><span class="sxs-lookup"><span data-stu-id="e6601-136">In :::no-loc(Q#):::, syntax elements are classified into three different groups: types, expressions, and statements.</span></span>

### <a name="types"></a><span data-ttu-id="e6601-137">Tipos</span><span class="sxs-lookup"><span data-stu-id="e6601-137">Types</span></span>
<span data-ttu-id="e6601-138">:::no-loc(Q#)::: é uma linguagem fortemente dactilografada, de modo que o uso cuidadoso de tipos pode ajudar o compilador a fornecer fortes garantias sobre :::no-loc(Q#)::: programas no tempo de compilação.</span><span class="sxs-lookup"><span data-stu-id="e6601-138">:::no-loc(Q#)::: is a strongly-typed language, such that careful use of types can help the compiler provide strong guarantees about :::no-loc(Q#)::: programs at compile time.</span></span>
<span data-ttu-id="e6601-139">Para além dos tipos primitivos integrados standard e específicos quânticos, por exemplo, `Int` `Bool` , `Qubit` `Result` :::no-loc(Q#)::: e, fornece suporte para tipos definidos pelo utilizador.</span><span class="sxs-lookup"><span data-stu-id="e6601-139">In addition to standard and quantum-specific built-in primitive types, for example, `Int`, `Bool`, `Qubit`, and `Result`, :::no-loc(Q#)::: provides support for user-defined types.</span></span>

<span data-ttu-id="e6601-140">Para descrições de todos os tipos primitivos, detalhes para tipos de matriz e tuple, e passos para definir novos tipos dentro de um :::no-loc(Q#)::: ficheiro, consulte [Tipos :::no-loc(Q#)::: em ](xref:microsoft.quantum.guide.types).</span><span class="sxs-lookup"><span data-stu-id="e6601-140">For descriptions of all the primitive types, details for array and tuple types, and steps to define new types within a :::no-loc(Q#)::: file, see [Types in :::no-loc(Q#):::](xref:microsoft.quantum.guide.types).</span></span>

### <a name="expressions"></a><span data-ttu-id="e6601-141">Expressões</span><span class="sxs-lookup"><span data-stu-id="e6601-141">Expressions</span></span>
<span data-ttu-id="e6601-142">Uma expressão numa linguagem de programação é uma combinação de uma ou mais constantes, variáveis, operadores e funções que a linguagem de programação interpreta e avalia a um valor específico.</span><span class="sxs-lookup"><span data-stu-id="e6601-142">An expression in a programming language is a combination of one or more constants, variables, operators, and functions that the programming language interprets and evaluates to a specific value.</span></span>
<span data-ttu-id="e6601-143">Mais simplesmente, para cada tipo de linguagem, expressões desse tipo podem ser *literais* ou símbolos ligados a um valor desse tipo.</span><span class="sxs-lookup"><span data-stu-id="e6601-143">Most simply, for every type in a language, expressions of that type can be either *literals* or symbols bound to a value of that type.</span></span>
<span data-ttu-id="e6601-144">Por exemplo, `5` é um `Int` literal (portanto, também uma expressão do `Int` tipo), e se o símbolo está `count` ligado ao valor `5` inteiro, então `count` é também uma expressão inteiro.</span><span class="sxs-lookup"><span data-stu-id="e6601-144">For example, `5` is an `Int` literal (thus also an expression of type `Int`), and if the symbol `count` is bound to the integer value `5`, then `count` is also an integer expression.</span></span>

<span data-ttu-id="e6601-145">Além disso, uma expressão pode consistir em outras expressões combinadas por certos operadores.</span><span class="sxs-lookup"><span data-stu-id="e6601-145">Additionally, an expression can consist of other expressions combined by certain operators.</span></span>
<span data-ttu-id="e6601-146">Por exemplo, outra `Int` expressão que avalia é `5` `2+3` .</span><span class="sxs-lookup"><span data-stu-id="e6601-146">For example, another `Int` expression that evaluates to `5` is `2+3`.</span></span>

<span data-ttu-id="e6601-147">Para obter mais informações sobre expressões e operadores compatíveis :::no-loc(Q#)::: em , consulte [Expressões tipo :::no-loc(Q#)::: em ](xref:microsoft.quantum.guide.expressions).</span><span class="sxs-lookup"><span data-stu-id="e6601-147">For more information about expressions and compatible operators in :::no-loc(Q#):::, see [Type Expressions in :::no-loc(Q#):::](xref:microsoft.quantum.guide.expressions).</span></span> 

### <a name="statements"></a><span data-ttu-id="e6601-148">Instruções</span><span class="sxs-lookup"><span data-stu-id="e6601-148">Statements</span></span> 
<span data-ttu-id="e6601-149">Uma declaração é uma unidade sintática de uma linguagem de programação imperativa que expressa alguma ação a realizar. As declarações contrastam com expressões em que as declarações não devolvem resultados e são executadas apenas pelos seus efeitos secundários.</span><span class="sxs-lookup"><span data-stu-id="e6601-149">A statement is a syntactic unit of an imperative programming language that expresses some action to carry out. Statements contrast with expressions in that statements do not return results and are run solely for their side effects.</span></span> <span data-ttu-id="e6601-150">As expressões, no entanto, devolvem sempre um resultado e muitas vezes não têm quaisquer efeitos colaterais.</span><span class="sxs-lookup"><span data-stu-id="e6601-150">Expressions, however, always return a result and often do not have any side effects.</span></span> <span data-ttu-id="e6601-151">Em suma, :::no-loc(Q#)::: as declarações são executadas, enquanto as expressões são avaliadas.</span><span class="sxs-lookup"><span data-stu-id="e6601-151">In short, :::no-loc(Q#)::: statements are run, while expressions are evaluated.</span></span>

<span data-ttu-id="e6601-152">Um simples exemplo de uma declaração :::no-loc(Q#)::: é atribuir um símbolo a uma expressão:</span><span class="sxs-lookup"><span data-stu-id="e6601-152">A simple example of a statement in :::no-loc(Q#)::: is assigning a symbol to an expression:</span></span>
```qsharp
let count = 5;
```

<span data-ttu-id="e6601-153">Um exemplo mais interessante é a `for` afirmação que suporta a iteração e inclui um *bloco de declaração.*</span><span class="sxs-lookup"><span data-stu-id="e6601-153">A more interesting example is the `for` statement which supports iteration and includes a *statement block* .</span></span>
<span data-ttu-id="e6601-154">Suponha que `qubits` seja o símbolo ligado a um registo de qubits (tecnicamente de `Qubit[]` tipo, ou uma matriz de `Qubit` tipos).</span><span class="sxs-lookup"><span data-stu-id="e6601-154">Suppose `qubits` is the symbol bound to a register of qubits (technically of type `Qubit[]`, or an array of `Qubit` types).</span></span> <span data-ttu-id="e6601-155">Então</span><span class="sxs-lookup"><span data-stu-id="e6601-155">Then</span></span>
```qsharp
for (qubit in qubits) {
    H(qubit);
}
```
<span data-ttu-id="e6601-156">é uma declaração que itera sobre cada qubit no registo, realizando a `H` operação em cada um.</span><span class="sxs-lookup"><span data-stu-id="e6601-156">is a statement that iterates over each qubit in the register, performing the `H` operation on each one.</span></span> <span data-ttu-id="e6601-157">Note que `H(qubit);` esta é uma afirmação em si mesma também.</span><span class="sxs-lookup"><span data-stu-id="e6601-157">Note that `H(qubit);` is a statement in itself as well.</span></span>

<span data-ttu-id="e6601-158">Pode utilizar qualquer expressão de chamada do tipo `Unit` (um `Unit` tipo não devolve nenhuma informação) como declaração.</span><span class="sxs-lookup"><span data-stu-id="e6601-158">You can use any call expression of type `Unit` (a `Unit` type does not return any information) as a statement.</span></span>
<span data-ttu-id="e6601-159">Este tipo de expressão é útil quando se chama operações em qubits que regressam `Unit` porque o objetivo da declaração é modificar o estado quântico implícito.</span><span class="sxs-lookup"><span data-stu-id="e6601-159">This type of expression is useful when calling operations on qubits that return `Unit` because the purpose of the statement is to modify the implicit quantum state.</span></span>
<span data-ttu-id="e6601-160">As declarações de avaliação de expressão requerem um ponto de terminação.</span><span class="sxs-lookup"><span data-stu-id="e6601-160">Expression evaluation statements require a terminating semicolon.</span></span>

<span data-ttu-id="e6601-161">Usas declarações para construir quase todos os aspetos de um :::no-loc(Q#)::: programa, e nenhuma página poderia abranger toda a informação que lhes diz respeito.</span><span class="sxs-lookup"><span data-stu-id="e6601-161">You use statements to build nearly every aspect of a :::no-loc(Q#)::: program, and no single page could encompass all the information relating to them.</span></span>
<span data-ttu-id="e6601-162">Para obter mais informações sobre a sua estrutura lexical e formatação, consulte [ :::no-loc(Q#)::: a Estrutura de Ficheiros;](xref:microsoft.quantum.guide.filestructure)para a atribuição e âmbito de ligação de [símbolos, consulte Variáveis :::no-loc(Q#)::: em ](xref:microsoft.quantum.guide.variables); e para ciclos de fluxo de controlo tais como , ver Control Flow in `for` [. :::no-loc(Q#)::: ](xref:microsoft.quantum.guide.controlflow)</span><span class="sxs-lookup"><span data-stu-id="e6601-162">For more information about their lexical structure and formatting, see [:::no-loc(Q#)::: File Structure](xref:microsoft.quantum.guide.filestructure); for symbol binding assignment and scope, see [Variables in :::no-loc(Q#):::](xref:microsoft.quantum.guide.variables); and for control flow loops such as `for`, see [Control Flow in :::no-loc(Q#):::](xref:microsoft.quantum.guide.controlflow).</span></span>

## <a name="next-steps"></a><span data-ttu-id="e6601-163">Passos seguintes</span><span class="sxs-lookup"><span data-stu-id="e6601-163">Next steps</span></span>

<span data-ttu-id="e6601-164">Comece a aprender sobre [tipos em :::no-loc(Q#)::: ](xref:microsoft.quantum.guide.types).</span><span class="sxs-lookup"><span data-stu-id="e6601-164">Start learning about [Types in :::no-loc(Q#):::](xref:microsoft.quantum.guide.types).</span></span>

<span data-ttu-id="e6601-165">Para mais informações sobre as fundações e motivação por :::no-loc(Q#)::: trás, veja [por que :::no-loc(Q#)::: precisamos?](https://devblogs.microsoft.com/qsharp/why-do-we-need-q/)</span><span class="sxs-lookup"><span data-stu-id="e6601-165">For more background about the foundations and motivation behind :::no-loc(Q#):::, see [Why do we need :::no-loc(Q#):::?](https://devblogs.microsoft.com/qsharp/why-do-we-need-q/).</span></span>
